$('<%= "#todo-#{@todo.id}" %>').replaceWith("<%= escape_javascript(render partial: 'todos/todo', locals: { list: @list, todo: @todo }) %>");
// post event to intercom
Intercom('trackEvent', 'updated-todo');